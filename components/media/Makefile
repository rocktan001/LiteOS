include $(LITEOSTOPDIR)/config.mk

MODULE_$(LOSCFG_COMPONENTS_LIBPNG) += libpng
MODULE_$(LOSCFG_COMPONENTS_OPUS) += opus
MODULE_$(LOSCFG_COMPONENTS_OPENEXIF) += openexif
MODULE_$(LOSCFG_COMPONENTS_UPNP) += upnp

include $(MODULE)
