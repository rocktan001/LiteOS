include $(LITEOSTOPDIR)/config.mk

MODULE_$(LOSCFG_COMPONENTS_SECURITY_MBEDTLS) += mbedtls
MODULE_$(LOSCFG_COMPONENTS_OPENSSL) += openssl
MODULE_$(LOSCFG_COMPONENTS_RHASH) += rhash
MODULE_$(LOSCFG_COMPONENTS_TINYCRYPT) += tinycrypt

include $(MODULE)
