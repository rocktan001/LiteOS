include $(LITEOSTOPDIR)/config.mk

MODULE_NAME := $(LITEOS_PLATFORM)
MODULE_y :=

MODULE_y += net
MODULE_y += connectivity
MODULE_$(LOSCFG_COMPONENTS_FS) += fs
MODULE_$(LOSCFG_COMPONENTS_GUI) += gui
MODULE_$(LOSCFG_COMPONENTS_LIB) += lib
MODULE_$(LOSCFG_COMPONENTS_OTA) += ota
MODULE_y += security
MODULE_$(LOSCFG_COMPONENTS_SENSORHUB) += sensorhub
MODULE_$(LOSCFG_COMPONENTS_AI) += ai
MODULE_y += utility
MODULE_y += media
MODULE_y += language

include $(MODULE)
