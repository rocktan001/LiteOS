include $(LITEOSTOPDIR)/config.mk

MODULE_$(LOSCFG_DEMOS_LIBPNG) += libpng
MODULE_$(LOSCFG_DEMOS_OPUS) += opus
MODULE_$(LOSCFG_DEMOS_OPENEXIF) += openexif
MODULE_$(LOSCFG_DEMOS_UPNP) += upnp

include $(MODULE)
